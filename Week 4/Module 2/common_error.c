#include <stdio.h>
int main()
{
    /*
        1. 1e10 --> bolte bujhai 1 er por 10 ta zero: 10000000000;


        2.
        int a = 6, b = 4;
        double c = a / b;

        printf("%lf\n", a / b); // Output: 0.000000 --> Aikhane 0 asar karon holo, upore a, b both int type. Jokhon devide kortesi, tokhon age result ta int type thaktese. But amra print kortesi lf dia. Jar fole value ta nitai partesena.

        printf("%lf\n", c);     // Output: 1.000000  --> Aikhane 1 asar karon holo, upore a, b both int type. Jokhon devide kortesi, tokhon age result ta int type convert hosse, tarpor print hosse. So 1.25.. ke 1 a convert kore dissa. Tar mane a, b er moddhe je kawke float hote hobe. Or devide korar somoi aivabe likhte pari: (double)a/b;  tahole a ke double dhore niba.

        3.
         int ans = 6 * 7 / 3 * 2;
        printf("%d", ans); // Output: 28 --> karon holo amra norlamly amth kora somoi age vag er kaj kori, tarpor mutliply er. But c programming a left site theke calculation suro hobe, seita vag thakuk ar jeitai thakuk. so age multiply hobe, tarpor 3 dia vag hobe, tarpor abar 2 dia multiply hobe.

        4.
        double a = 2.5;
        double b = 1.5;
        printf("%d ", a % b); // aikhane error diba. Karon kono 2 ta number ke modulas korle, all time int type valeu return korbe, but aikhane amra lf type dia rakhsi.

    */

    return 0;
}